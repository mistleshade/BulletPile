using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NCMB;
public class TestPost : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Type == Number の場合
       

        // クラスのNCMBObjectを作成
        //NCMBObject testClass = new NCMBObject("Score");

        // オブジェクトに値を設定

      //  testClass["score"] = 40;
        // データストアへの登録
       //testClass.SaveAsync();
    }

}
